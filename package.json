{
  "name": "example-percy-maestro",
  "version": "0.0.1",
  "private": true,
  "description": "Example integration of Percy visual testing with the Maestro web testing framework",
  "scripts": {
    "test-web": "percy exec -- bash -c 'percy-maestro serve & SRVPID=$!; sleep 2; maestro test flows/web-percy.yaml; kill $SRVPID 2>/dev/null; wait 2>/dev/null'",
    "clean": "rm -rf ./screenshots"
  },
  "devDependencies": {
    "@percy/cli": "^1.31.2",
    "@percy/maestro": "file:../percy-maestro"
  },
  "engines": {
    "node": ">=14"
  }
}
